import 'package:flutter/material.dart';

class HomeGuestInfoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HomeGuestInfo'),
      ),
    );
  }
}
