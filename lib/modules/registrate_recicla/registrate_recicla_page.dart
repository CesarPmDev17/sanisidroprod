import 'package:flutter/material.dart';

class RegistrateReciclaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RegistrateReciclaPage'),
      ),
    );
  }
}
